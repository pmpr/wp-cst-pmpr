{% extends get_template('information_table', _self) %}

{% block item_content %}
    {% if item.description is not empty %}
        <tr class="font-16">
            <td class="text-gray-700 font-weight-bold py-2 px-3 text-right">{{ item.description|raw }}</td>
        </tr>
    {% endif %}
{% endblock %}

